name: Build Xiaomi CC9 Kernel

on:
  workflow_dispatch:

permissions:
  contents: read

env:
  KERNEL_REPO: https://github.com/MiCode/Xiaomi_Kernel_OpenSource.git
  KERNEL_BRANCH: pyxis-p-oss
  DEFCONFIG: pyxis_user_defconfig

jobs:
  build:
    name: Build pyxis kernel
    runs-on: ubuntu-22.04

    # 官方源码中的 gcc-wrapper.py 使用 Python 2，
    # 同时老内核与 Ubuntu 20.04 的 OpenSSL 环境兼容性更好。
    container:
      image: ubuntu:20.04

    steps:
      - name: Install build dependencies
        shell: bash
        run: |
          export DEBIAN_FRONTEND=noninteractive

          apt-get update

          apt-get install -y \
            bc \
            bison \
            build-essential \
            ca-certificates \
            ccache \
            curl \
            flex \
            git \
            libelf-dev \
            libssl-dev \
            libncurses5-dev \
            libncurses-dev \
            liblz4-tool \
            lzop \
            make \
            python2 \
            python-is-python2 \
            rsync \
            unzip \
            wget \
            zip

      - name: Clone Xiaomi kernel source
        shell: bash
        run: |
          set -e

          git clone \
            --depth=1 \
            --single-branch \
            --branch "${KERNEL_BRANCH}" \
            "${KERNEL_REPO}" \
            kernel

      - name: Clone ARM64 GCC 4.9 toolchain
        shell: bash
        run: |
          set -e

          git clone \
            --depth=1 \
            --single-branch \
            --branch lineage-19.1 \
            https://github.com/LineageOS/android_prebuilts_gcc_linux-x86_aarch64_aarch64-linux-android-4.9.git \
            gcc64

      - name: Clone ARM32 GCC 4.9 toolchain
        shell: bash
        run: |
          set -e

          git clone \
            --depth=1 \
            --single-branch \
            --branch lineage-19.1 \
            https://github.com/LineageOS/android_prebuilts_gcc_linux-x86_arm_arm-linux-androideabi-4.9.git \
            gcc32

      - name: Show compiler version
        shell: bash
        run: |
          ./gcc64/bin/aarch64-linux-android-gcc --version
          ./gcc32/bin/arm-linux-androideabi-gcc --version

      - name: Generate kernel config
        working-directory: kernel
        shell: bash
        run: |
          set -e

          export ARCH=arm64
          export SUBARCH=arm64

          export CROSS_COMPILE="${GITHUB_WORKSPACE}/gcc64/bin/aarch64-linux-android-"
          export CROSS_COMPILE_ARM32="${GITHUB_WORKSPACE}/gcc32/bin/arm-linux-androideabi-"

          make O=out "${DEFCONFIG}"

          echo "Kernel configuration generated:"
          grep -E "CONFIG_BUILD_ARM64|CONFIG_LOCALVERSION" out/.config || true

      - name: Build kernel
        working-directory: kernel
        shell: bash
        run: |
          set -o pipefail

          export ARCH=arm64
          export SUBARCH=arm64

          export CROSS_COMPILE="${GITHUB_WORKSPACE}/gcc64/bin/aarch64-linux-android-"
          export CROSS_COMPILE_ARM32="${GITHUB_WORKSPACE}/gcc32/bin/arm-linux-androideabi-"

          make \
            O=out \
            -j"$(nproc)" \
            2>&1 | tee "${GITHUB_WORKSPACE}/build.log"

      - name: Collect build outputs
        if: always()
        shell: bash
        run: |
          mkdir -p artifacts/kernel
          mkdir -p artifacts/dtb
          mkdir -p artifacts/modules

          if [ -d kernel/out/arch/arm64/boot ]; then
            find kernel/out/arch/arm64/boot \
              -maxdepth 1 \
              -type f \
              -name 'Image*' \
              -exec cp -v {} artifacts/kernel/ \;

            find kernel/out/arch/arm64/boot/dts \
              -type f \
              \( -name '*.dtb' -o -name '*.dtbo' \) \
              -exec cp -v {} artifacts/dtb/ \; \
              2>/dev/null || true
          fi

          if [ -d kernel/out ]; then
            find kernel/out \
              -type f \
              -name '*.ko' \
              -exec cp -v --parents {} artifacts/modules/ \; \
              2>/dev/null || true
          fi

          if [ -f kernel/out/.config ]; then
            cp kernel/out/.config artifacts/kernel/config
          fi

          cp build.log artifacts/ 2>/dev/null || true

          echo "Collected files:"
          find artifacts -type f -print || true

      - name: Verify kernel image
        if: success()
        shell: bash
        run: |
          if ! find artifacts/kernel -maxdepth 1 -type f -name 'Image*' | grep -q .; then
            echo "Kernel image was not generated."
            exit 1
          fi

      - name: Upload kernel artifact
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: Xiaomi-CC9-pyxis-kernel-${{ github.run_number }}
          path: artifacts/
          if-no-files-found: warn
          retention-days: 14
